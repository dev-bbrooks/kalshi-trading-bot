Dashboard Fix Pass 3 — Targeted Remaining Issues

The stats navigation and command structure are fixed. These are the specific remaining problems. For each one, I’m telling you exactly what to do.

1. HOME TAB STAT GRID — STILL HAS SESSION DATA
   The Home tab stat grid still shows session stats (wins, losses, WR, P&L in row 1, then streaks and peak/drawdown in row 2). This MUST show LIFETIME stats only. The stat grid HTML should be:

Row 1: Lifetime Wins, Lifetime Losses, Win Rate (lifetime), Total P&L (lifetime)
That’s it. No row 2. No streaks. No peak P&L. No drawdown.

The renderUI function should read lifetime_wins and lifetime_losses from state (state.lifetime_wins, state.lifetime_losses, state.lifetime_pnl), NOT session_wins/session_losses.

Remove: _sessTrack object, _initSessionStats function, all streak tracking JS, statWinStreak/statLossStreak/statPeakPnl/statDrawdown elements.

Update the stat grid HTML to have 4 cells: Wins | Losses | WR | P&L
Update renderUI to populate from state.lifetime_wins, state.lifetime_losses, state.lifetime_pnl.

1. LIVE PRICE CHART STYLE
   Open legacy/dashboard.py and search for “_drawLiveChart” or “liveChart” or “livePriceBuf”. Find the EXACT canvas drawing function that renders the YES/NO price lines. It draws on a <canvas> element with:

- ctx.strokeStyle for green (YES) and red (NO) lines
- ctx.lineWidth = 1.5 or similar thin lines
- Background is transparent (canvas sits on dark card)
- Y-axis labels at 0, 25, 50, 75, 100 cents
- Horizontal grid lines at those levels (subtle, using rgba)
- The line data comes from _livePriceBuf which accumulates price points

Copy this EXACT function into the plugin JS. Don’t simplify it.

1. BTC CANDLE CHART
   Open legacy/dashboard.py and search for “renderBtcChart” or “_drawBtcCandles” or “btcChartCanvas”. Find the canvas rendering that draws proper candlesticks. It should draw:

- Green filled rectangles for up candles (close > open)
- Red filled rectangles for down candles (close < open)
- Thin vertical lines for wicks (high to low)
- Dark background
- Price axis on right

Copy this EXACT function. The current version is likely a simplified line chart — replace it with the real candlestick renderer.

1. SERVER STATS PROGRESS BARS
   Open legacy/dashboard.py and search for “loadSystemStats” or “system_stats” or “sysStatsCpu”. Find the rendering code that creates progress bar HTML for CPU, Memory, and Disk. The pattern is:

<div style="...bar container...">
  <div style="width:XX%;background:COLOR;...bar fill..."></div>
</div>
Where COLOR is green (<70%), yellow (70-90%), red (>90%).

Copy this exact rendering into the platform dashboard’s system stats section. The current version just shows text values without bars.

1. FILTER CHIPS — THREE-STATE CYCLING
   The trade filter chips must cycle through three states on tap:

- Default (no filter): gray border, dim text
- Include: colored border (blue, green, red depending on group), colored text
- Exclude: red background tint, strikethrough or X indicator

Open legacy/dashboard.py and search for “setTradeFilter” or “tradeFilterColors” or the filter chip onclick handler. The old version used a Map with three states: unset -> ‘include’ -> ‘exclude’ -> unset. Each state had different CSS classes.

If the current implementation only has two states (on/off), replace it with the three-state version from legacy.

1. BANKROLL MODAL CHARTS
   The bankroll modal should have two canvas charts:

- Bankroll over time (line chart)
- P&L per trade (bar or dot chart)

Open legacy/dashboard.py and search for “loadBankrollChart” or “bkmChart” or “bankrollChartCanvas”. Copy the chart rendering. Make sure the API calls use the new endpoint paths with plugin_id parameter.

1. PULL-TO-REFRESH
   If pull-to-refresh is not working on iOS, open legacy/dashboard.py and search for “_ptr” or “pullRefresh” or the touchstart/touchmove/touchend handlers on contentWrap. The implementation involves:

- touchstart: record start Y position, only if scroller.scrollTop === 0
- touchmove: calculate distance, apply resistance curve, show progress bar
- touchend: if past threshold, trigger refresh
- Must be disabled when modal is open or touching a canvas/chart

Copy the exact implementation.

1. TRADE DETAIL MODAL
   When tapping a trade card, a detail modal should open showing:

- Full trade details (all fields)
- Price path chart (canvas) if price path data exists
- Action buttons (delete)

Open legacy/dashboard.py and search for “showTradeDetail” or “tradeDetailOverlay” or “tradeDetailContent”. Copy the function that populates the modal with trade data and renders the price path chart.

1. REGIME DETAIL MODAL
   When tapping a regime card in the Regimes tab, a detail modal or expandable section should show per-regime stats and configuration. Check if this exists and works. Open legacy/dashboard.py and search for “showRegimeDetail”.
1. CONFIG LOADING ON SETTINGS TAB
   When switching to the Settings tab, all input fields should populate with current config values. Open legacy/dashboard.py and search for “loadConfig” or the function that reads /api/config and populates form inputs. The new version must:

- Fetch config
- Strip btc_15m. prefix from keys
- Set input values, checkbox states, select values
  Verify every settings input actually loads its current value.

1. SERVICE NAMES IN SETTINGS
   The services card must use new supervisor names. Search the plugin and platform code for “kalshi-bot” or “kalshi-dashboard” and replace with “plugin-btc-15m” and “platform-dashboard”. Also check the valid_services set in the /api/server/control route.
1. OBSERVATION COUNT IN HOME TAB
   The Home tab should show observatory health: total observations recorded, capture rate. The old dashboard showed this near the bottom of the Home tab. Check if it exists and if it reads from the correct state field (state.observatory_health or a separate API call to /api/btc_15m/observation_count).

For ALL visual items: read the legacy/dashboard.py implementation and copy it exactly. Don’t approximate. The old code is the spec.

py_compile after all changes. supervisorctl restart platform-dashboard