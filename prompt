Dashboard Fix Pass 2 — Remaining Issues

Read this entire list, then fix each item. For every visual issue, read the corresponding section in legacy/dashboard.py and copy faithfully.

STILL-PRESENT REMOVED FEATURES:

1. Session tracking still in plugin JS — The renderUI function still references _sessTrack.maxWinStreak, _sessTrack.maxLossStreak, _sessTrack.peakPnl, session_wins, session_losses, session_pnl, session_skips. The _sessTrack object, _initSessionStats function, and ALL session stat display elements must be removed. The Home tab stat grid should show LIFETIME stats only (wins, losses, win rate, P&L) — NOT session stats. Read the Home stat grid HTML and remove any row that shows session data, streak data, peak P&L, or drawdown.
1. Old stats sub-pages still in navigation — The statsNavTo titles object still includes observatory, models, validation, convergence. Remove these entries. Only keep: performance, regimes, shadow. Also remove the corresponding _statsRenderObservatory, _statsRenderModels, _statsRenderValidation, _statsRenderConvergence functions and any _statsLoadPage case statements for them. The Stats hub should only show links/buttons for: Performance, Regime Analysis, Shadow Trading.
1. Market Conditions sub-page — Check if there’s a “conditions” sub-page. This was in the old dashboard. It can stay if it works (it shows breakdowns by hour, side, volatility, etc.) but make sure it fetches from /api/btc_15m/performance (not /api/performance). If broken, remove it.
1. Cash out overlay — Search the plugin HTML for any cash out overlay, cash out button, or confirmCashOut/hideCashOut/cancelCashOut functions. Remove all of them.
1. Reset Session overlay — Search for resetSessionOverlay, doResetSession, recoverSession. Remove all session reset UI.
1. Loss streak reset — Search for resetStreakOverlay, confirmResetStreak, doResetStreak. If loss streak tracking was removed from bot.py, remove these from the dashboard too.

VISUAL ISSUES THAT NEED LEGACY CODE COPY:

1. Server stats card — Must have progress bars with color thresholds, not just text values. Read legacy/dashboard.py, search for “api/system_stats” and the loadSystemStats/renderSystemStats function. Copy the HTML that renders CPU/Memory/Disk with progress bar elements and color-coded fills (green < 70%, yellow 70-90%, red > 90%). The old code generates something like:
   
   <div class="progress-bar"><div class="progress-fill" style="width:70%;background:var(--green)"></div></div>
   Copy that exact pattern.
1. Live price chart canvas rendering — The Home tab live chart must use canvas rendering matching the old dashboard. Search legacy/dashboard.py for _drawLiveChart or the canvas rendering code that draws YES (green line) and NO (red line) prices over time. Key details:
- Canvas with dark background (transparent or –bg)
- 1px thin lines, anti-aliased
- Y-axis price labels (0-100 cents)
- X-axis time remaining
- Green line = YES ask, Red line = NO ask
- Subtle grid lines at 25c intervals
  Copy the exact drawing function.
1. BTC candle chart — Search legacy/dashboard.py for renderBtcChart or _drawBtcCandles. The old chart draws proper candlesticks with wicks. Copy the canvas rendering code. Don’t use a simplified version.
1. Bankroll modal charts — The bankroll modal should have two charts: bankroll history (line chart) and P&L history (bar or line chart). Search legacy/dashboard.py for loadBankrollChart or the bankroll modal chart code. Copy it. Make sure the API endpoints are /api/bankroll/chart and /api/pnl/chart with plugin_id parameter.
1. Trade filter chips — The three-state filter system must cycle: default (gray) -> include (blue/green border) -> exclude (red border) -> default. Search legacy/dashboard.py for the filter chip cycling logic. Each filter group has specific colors. The old dashboard had 17 filter dimensions across 6 groups. Copy the filter chip HTML and the cycling JS exactly.
1. Pull-to-refresh — If not implemented, copy from legacy/dashboard.py. Search for “pull” or “pullToRefresh” or “_ptr”. The implementation involves touch event handlers on contentWrap, a progress bar element, resistance curve calculation, and threshold detection. Critical: must check scroller.scrollTop, not window.scrollY.

FUNCTIONAL ISSUES:

1. Services card service names — The services card in Settings must use the new supervisor program names: “platform-dashboard” and “plugin-btc-15m”. NOT the old “kalshi-bot” and “kalshi-dashboard”. Search the settings HTML and the svcControl function for service names and update them. Also update the valid_services set in the /api/server/control route in platform dashboard.py.
1. Deploy upload service restart — After deploying files, the restart should use the new service names. Check the deploy endpoint and the JS that calls it.
1. Config saving — When settings are saved, the key must be namespaced. Example: saving bet_size should call saveSetting(‘btc_15m.bet_size’, value), not saveSetting(‘bet_size’, value). Check every saveSetting call in the plugin JS to ensure it uses the btc_15m. prefix.
1. Strategy picker — The strategy picker (side, sell target, entry max, timing selectors) must save to namespaced config keys. Verify _applyStrategyPicker saves btc_15m.strategy_side, btc_15m.sell_target_c, btc_15m.entry_price_max_c, btc_15m.entry_delay_minutes.
1. Regime config saving — Risk level actions and per-regime overrides must save to btc_15m.risk_level_actions and btc_15m.regime_overrides. Check the setRiskAction function and any regime override save handlers.
1. Auto-strategy settings — The auto-strategy min samples, min EV, and fee buffer inputs must save to btc_15m.auto_strategy_min_samples, etc.

POLISH:

1. Tab switching scroll reset — When switching tabs, scroll the contentWrap to top. The switchTab function should call scrollTop() or set contentWrap.scrollTop = 0.
1. Skeleton loading — Each tab should show skeleton loading placeholders that fade out when real data arrives. Check that hideSkel is called for each tab’s skeleton on data load.
1. Status indicator in header — The status dot/text in the header should show the current state: green dot when observing, blue when trading, red when stopped, etc. Verify the renderUI function updates the status indicators in the header.
1. Regime worker status — The Regimes tab should show regime worker status (running/stopped, candle count, last snapshot time). Verify loadRegimeWorkerStatus fetches from the correct endpoint and displays the data. The old dashboard had a regime engine status card showing phase, candle count, and last snapshot.

Fix all items. For visual issues (7-12), you MUST read the legacy/dashboard.py code and copy the exact implementation. Don’t approximate or simplify.

py_compile after all changes. supervisorctl restart platform-dashboard to test.